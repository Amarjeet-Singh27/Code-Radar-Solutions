#include <stdio.h>

int main() {
    int n1, n2,n3;
    scanf("%d %d %d",&n1,&n2,&n3);
    if((a + b > c) && (a + c > b) && (c + b > a)){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    return 0;
}