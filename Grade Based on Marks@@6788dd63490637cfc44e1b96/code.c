#include <stdio.h>

int main() {
    int num;
    scanf("%d", &num);
    if (num >= 90){
        printf("A";)
    }
    if ( 90 > num >= 80){
        printf("B");
    }
    if ( 80 > num >= 70)(
        printf("c");
    )
    if ( 70 > num >= 60){
        printf("D");
    }
    if ( num < 60){
        printf("F");
    }
    return 0;
}