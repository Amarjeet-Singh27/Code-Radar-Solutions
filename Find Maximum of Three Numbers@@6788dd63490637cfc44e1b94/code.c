#include <stdio.h>

int main() {
    int n1, n2, n3;
    scanf("%d %d %d", &n1, &n2, &n3);
    if((a > b)&&(a > b)){
        printf("%d",n1);
    }
    else if ((b > c) && (b > a)){
        printf("%d", n2);
    }
    else {
        printf("%d", n3);
    }
    return 0;
}