#include <stdio.h>

int main() {
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    if((a > b)&&(a > b)){
        printf("%d",n1);
    }
    else if ((b > c) && (b > a)){
        printf("%d", n2);
    }
    else {
        printf("%d", n3);
    }
    return 0;
}